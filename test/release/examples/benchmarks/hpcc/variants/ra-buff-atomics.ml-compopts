#!/usr/bin/env python

# TODO update this comment and the filenames below

# Run ra with n=33 and 10 million updates. ugni is much faster for ra-atomics,
# so bump the number of updates to 1 billion to get stable timings.
import os

comm = os.getenv('CHPL_COMM')
ugni = comm == 'ugni'

nu = "2**(n-10)"
if ugni:
  nu = "2**(n-5)"

print('-sN_U={0} -sverify=false # ra.buff-atomic-n33'.format(nu))
